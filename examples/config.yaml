# Minimal v1 development configuration.
version: 1

listeners:
  mqtt:
    - address: "127.0.0.1:1883"
      transport: tcp
      versions: ["3.1.1", "5.0"]
  amqp091: []
  amqp1: []

storage:
  type: memory
